auto-install-peers=false
strict-peer-dependencies=false

# Expo / React Native require a flat (hoisted) node_modules layout so that
# packages like @expo/metro-config can resolve expo-asset, expo, etc.
# https://docs.expo.dev/guides/monorepos/#common-issues
node-linker=hoisted
