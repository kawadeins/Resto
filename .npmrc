auto-install-peers=false
strict-peer-dependencies=false

# Expo / React Native require expo-asset to be resolvable at the root level.
# shamefully-hoist hoists all packages to root node_modules (keeps isolated linker).
# https://docs.expo.dev/guides/monorepos/#common-issues
shamefully-hoist=true
